#include<bits/stdc++.h>
using namespace std;

int longestSubsequence(vector<int>&vec) {
    int ans  = 0;

    f
}



int main() {
    int n; cin >> n;

    vector<int>vec(n);

    for(int i = 0; i < n; i++) cin >> vec[i];

    cout << longestSubsequence(vec);
}