# Ignore all files that do not contain a dot (i.e., no extension)
# Matches files like: main, a.out, run, bin (not folders)
*

# But don't ignore files with extensions (like source files)
!*.cpp
!*.c
!*.h
!*.hpp
!*.txt
!*.md
!*.gitignore

# Also don't ignore folders
!*/

# Ignore VS Code config
.vscode/
